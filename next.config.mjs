/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['host.docker.internal', '147.78.131.42'], // Add your hostname here
    },
  };
  
  export default nextConfig;